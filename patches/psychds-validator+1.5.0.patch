diff --git a/node_modules/psychds-validator/esm/src/utils/platform.js b/node_modules/psychds-validator/esm/src/utils/platform.js
index defd0bc..b46b831 100644
--- a/node_modules/psychds-validator/esm/src/utils/platform.js
+++ b/node_modules/psychds-validator/esm/src/utils/platform.js
@@ -27,8 +27,8 @@ export const path = {
     resolve: (...paths) => _path ? _path.resolve(...paths) : paths.join("/"),
     /** Gets the last portion of a path */
     basename: (path, ext) => _path ? _path.basename(path, ext) : path.split("/").pop() || "",
-    /** Joins all given path segments together */
-    join: (...paths) => _path ? _path.join(...paths) : paths.join("/"),
+    /** Joins all given path segments together (always POSIX-style; collapses duplicate slashes) */
+    join: (...paths) => paths.join("/").replace(/\/+/g, "/") || "/",
     /** Returns the directory name of a path */
     dirname: (path) => _path ? _path.dirname(path) : path.split("/").slice(0, -1).join("/"),
     /** Platform-independent path separator */
diff --git a/node_modules/psychds-validator/script/src/utils/platform.js b/node_modules/psychds-validator/script/src/utils/platform.js
index 95d3586..e9f2f11 100644
--- a/node_modules/psychds-validator/script/src/utils/platform.js
+++ b/node_modules/psychds-validator/script/src/utils/platform.js
@@ -57,8 +57,8 @@ exports.path = {
     resolve: (...paths) => _path ? _path.resolve(...paths) : paths.join("/"),
     /** Gets the last portion of a path */
     basename: (path, ext) => _path ? _path.basename(path, ext) : path.split("/").pop() || "",
-    /** Joins all given path segments together */
-    join: (...paths) => _path ? _path.join(...paths) : paths.join("/"),
+    /** Joins all given path segments together (always POSIX-style; collapses duplicate slashes) */
+    join: (...paths) => paths.join("/").replace(/\/+/g, "/") || "/",
     /** Returns the directory name of a path */
     dirname: (path) => _path ? _path.dirname(path) : path.split("/").slice(0, -1).join("/"),
     /** Platform-independent path separator */
