---
"@jspsych/metadata-cli": patch
---

Add unit tests for `preAnalyzeDirectory` in `data.ts`, covering unreadable directories, JSON and CSV duplicate detection, ignored files, worst-file selection, one-subdirectory-deep traversal, and custom join keys.
